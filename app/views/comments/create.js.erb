$("#comment_list").html("<% @note.comments.each do |comment| %><p><strong>• </strong><%= comment.name %><br><%= comment.body %><% if comment.user_id == current_user.id %><%= link_to "Edit", edit_note_comment_path(@note, comment) %><%= link_to "Delete",  note_comment_path(@note, comment),method: :delete,data: { confirm: "Are you sure?" } %><% end %></p><% end %><%= j render :partial => 'user_comment', locals: {note: @note} %>")